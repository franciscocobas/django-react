# Django & React App

## installation

`pip install django`
`pip install djangorestframework`

##Run server
`cd music_controller`
`python3 manage.py runserver`
`cd music_controller/frontend`
`npm run dev`
